<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
    </head>

    <body>
        <h1>EJS WORKS</h1>
        <table>
            <tr>
                <th>Number</th>
                <th>Running?</th>
                <th>Result</th>
                <th>Version</th>
            </tr>
            <% for(let i = 0; i < 10; i++){ %>
            <tr>
                <td><%= builds_array[i].build_num %></td>
                <td><%= builds_array[i].running %></td>
                <td><%= builds_array[i].result %></td>
                <td><%= builds_array[i].version %></td>
                <td><a href="new">Other page</a></td>
            </tr>
            <% } %>
        </table>
        
        
    </body>
</html>